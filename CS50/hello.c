#include<stdio.h>
int main(void){
    int count = 1;
    while(count>0){
        printf("%d ",count);
        count++;
    } 
    printf("Hello World!");
}
//up key just paste's the last command given in terminal
//up key two times gives second last command